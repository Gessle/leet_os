/*int spawn_new_vpu(void);
int init_vpu(struct vpu *vpu, unsigned short stack_size, unsigned short code_size, unsigned short code_count, unsigned short data_size, unsigned short data_count, unsigned short interrupt_count);
extern unsigned char *message;
#include "vpu/loadprog.c"

int load_vpu_prog(char *filename)
{
  int cvpu = spawn_new_vpu();

  load_program(filename, &vpus[cvpu]);



  return 0;
}*/
